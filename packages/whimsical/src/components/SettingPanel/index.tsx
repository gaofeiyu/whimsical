import Panel from '../Panel';

const SettingPanel = () => {
  return (
    <div className="flex">
      <Panel title="属性配置属性配置属性配置">test</Panel>
    </div>
  );
};

SettingPanel.displayName = 'SettingPanel';

export default SettingPanel;
